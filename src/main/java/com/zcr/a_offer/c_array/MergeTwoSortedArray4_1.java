package com.zcr.a_offer.c_array;

public class MergeTwoSortedArray4_1 {

    /*public int[] merge(int[] a,int[] b) {
        int alen = a.length;
        int blen = b.length;
        int apoint = alen - 1;
        int bpoint = blen - 1;
        int
        for (; bpoint >= 0; bpoint--) {
            if (b[bpoint] == a[apoint]) {
                a[apoint--] = b[bpoint];
            } else if (b[bpoint] > apoint[apoint]) {

            }
        }
    }*/
}
